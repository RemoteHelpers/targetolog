jQuery(document).ready(function($) {
  $('.menu-icon').on('click', function() {
    $('.nav').toggleClass('active')
    $('.content').toggleClass('blurry');
    $('.logo').toggleClass('blurry');
  });
});